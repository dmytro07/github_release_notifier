import { describe, it } from 'vitest';

describe('GitHubClient', () => {
  it.todo('should verify a repository exists');
  it.todo('should return false for non-existent repository');
  it.todo('should fetch the latest release tag');
  it.todo('should return null when no releases exist');
});
